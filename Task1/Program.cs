using System;
// Console.WriteLine("Hello, World!");

internal class Program
{
    static void Main()
    {
        // int number = 9542; // % остаток от деления - вывод последней цифры
        // int lastDigit = number % 10; // / деление - удаление последней цифры
        // // Console.WriteLine(lastDigit);

        // Console.WriteLine(lastDigit); // shift + alt + f (наводит порядок в тексте)


        // number = number / 10; // num =>954
        // lastDigit = number % 10; 
        // Console.WriteLine(lastDigit);

        // number = number / 10; // num =>95
        // lastDigit = number % 10; 
        // Console.WriteLine(lastDigit);

        // number = number / 10; // num =>9
        // lastDigit = number % 10; 
        // Console.WriteLine(lastDigit);


        long number = 5000000000;
        int lastDigit;

        while (?????)

        lastDigit = number % 10;
        Console.WriteLine(lastDigit);
        number = number / 10; // num =>954

        lastDigit = number % 10;
        Console.WriteLine(lastDigit);
        number = number / 10; // num =>954

        lastDigit = number % 10;
        Console.WriteLine(lastDigit);
        number = number / 10; // num =>954

        lastDigit = number % 10;
        Console.WriteLine(lastDigit);
        number = number / 10; // num =>954
        


    }
}